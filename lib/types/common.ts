export type Id = string;

/** Sempre produzido via new Date(...).toISOString() */
export type ISODateString = string;

export type Turno = "manha" | "tarde" | "noite";

export type Perfil = "operador" | "supervisor" | "manutencao" | "admin";
